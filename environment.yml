name: anatprep
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.11
  - nibabel>=5.0
  - nilearn>=0.10
  - numpy>=1.20
  - scipy>=1.10
  - click>=8.0
  - pyyaml>=6.0
  - matplotlib
  - pip
  - pip:
      - pymp2rage  # try direct install first; fallback: vendor the math
