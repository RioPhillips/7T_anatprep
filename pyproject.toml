[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "anatprep"
version = "0.1.0"
description = "Anatomical preprocessing pipeline for 7T MP2RAGE data"
readme = "README.md"
authors = [
    {name = "Lund University 7T MRI Group"}
]
requires-python = ">=3.10"
classifiers = [
    "Development Status :: 3 - Alpha",
    "Operating System :: POSIX :: Linux",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
]
keywords = ["BIDS", "MRI", "neuroimaging", "7T", "MP2RAGE", "fMRIprep"]

dependencies = [
    "click>=8.0",
    "nibabel>=5.0",
    "nilearn>=0.10",
    "numpy>=1.20",
    "scipy>=1.10",
    "pyyaml>=6.0",
]

[project.optional-dependencies]
nighres = [
    "psutil",
    "antspyx",
    "dipy",
]
dev = [
    "pytest>=7.0",
    "pytest-cov",
    "black",
    "flake8",
]

[project.urls]
Homepage = "https://github.com/RioPhillips/anatprep"
Repository = "https://github.com/RioPhillips/anatprep.git"

[project.scripts]
anatprep = "anatprep.cli:main"
pymp2rage = "anatprep.cli:pymp2rage_cmd"
mask = "anatprep.cli:mask_cmd"
denoise = "anatprep.cli:denoise_cmd"
cat12 = "anatprep.cli:cat12_cmd"
nighres-dura = "anatprep.cli:nighres_dura_cmd"
nighres-skullstrip = "anatprep.cli:nighres_skullstrip_cmd"
sinus-auto = "anatprep.cli:sinus_auto_cmd"
sinus-edit = "anatprep.cli:sinus_edit_cmd"
brainmask-edit = "anatprep.cli:brainmask_edit_cmd"
run-freesurfer = "anatprep.cli:freesurfer_cmd"

[tool.setuptools.packages.find]
where = ["."]
include = ["anatprep*"]

[tool.setuptools.package-data]
"anatprep.scripts" = ["*.sh", "*.m"]
"anatprep.scripts.helpers" = ["*.m"]

[tool.black]
line-length = 100
target-version = ['py310', 'py311']

[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = ["test_*.py"]